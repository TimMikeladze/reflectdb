{
  "name": "reflectdb-landing",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "og": "bun run scripts/og.ts"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^5.4.10"
  }
}
